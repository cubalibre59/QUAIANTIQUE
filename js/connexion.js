alert('connexion.js est chargé');


console.log('connexion.js chargé et fonctionne');



// references DOM
window.addEventListener('DOMContentLoaded', () => {
  // Aquí va TODO tu código JS que manipula el DOM, como getElementById, addEventListener, etc.
});

    const form = document.getElementById('loginForm');
    const logoutSection = document.getElementById('logoutSection');
    const userDisplay = document.getElementById('userDisplay');
    const logoutBtn = document.getElementById('logoutBtn');
    console.log({ form, logoutSection, userDisplay, logoutBtn });
 
  